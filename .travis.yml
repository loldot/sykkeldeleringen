matrix:
  include:
    - language: csharp
      mono: none
      dotnet: 2.1.4
      before_script:
        - cd webapi
      script: dotnet build
    - language: node_js
      node_js: 8
      cache:
      directories:
        - node_modules
      before_script:
        - cd client
        - npm i g @angular/cli
      script: npm run build